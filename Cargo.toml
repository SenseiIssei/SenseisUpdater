[workspace]
resolver = "2"
members = [
    "crates/odysync-core",
    "crates/odysync-verify",
    "crates/odysync-backends",
    "crates/odysync-version-source",
    "crates/odysync-cli",
    "apps/gui/src-tauri",
]

[workspace.package]
version = "2.2.0"
edition = "2021"
rust-version = "1.82"
license = "MIT"
repository = "https://github.com/SenseiIssei/Odysync"
authors = ["Jakob (SenseiIssei)"]

[workspace.dependencies]
odysync-core = { path = "crates/odysync-core" }
odysync-verify = { path = "crates/odysync-verify" }
odysync-backends = { path = "crates/odysync-backends" }
odysync-version-source = { path = "crates/odysync-version-source" }

anyhow = "1"
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "process", "time", "sync", "io-util", "fs"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
clap = { version = "4", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
sha2 = "0.10"
hex = "0.4"
directories = "5"
async-trait = "0.1"
zip = { version = "2", default-features = false, features = ["deflate"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
scraper = "0.20"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
