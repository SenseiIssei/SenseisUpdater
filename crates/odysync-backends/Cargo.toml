[package]
name = "odysync-backends"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
# Not published to crates.io; also lets cargo-deny's allow-wildcard-paths
# apply to our path dependencies on each other.
publish = false
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Package manager integrations for Odysync"

[dependencies]
odysync-core.workspace = true
odysync-verify.workspace = true
async-trait.workspace = true
tokio.workspace = true
tracing.workspace = true
serde.workspace = true
serde_json.workspace = true
futures = { version = "0.3", default-features = false, features = ["std", "async-await"] }
unicode-width = "0.2"
zip.workspace = true
chrono.workspace = true
regex = "1"
reqwest.workspace = true
sha2.workspace = true
hex.workspace = true
directories.workspace = true
anyhow.workspace = true

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62", features = [
    "Win32_Foundation",
    "Win32_System_Com",
    "Win32_System_UpdateAgent",
    # `cleanup` asks the shell for the Recycle Bin's size and empties it.
    # Walking `C:\$Recycle.Bin` instead would under-report (it is per-user and
    # permission-guarded) and is exactly the shape the root-safety rule refuses.
    "Win32_UI_Shell",
] }

[dev-dependencies]
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
tempfile = "3"
